# azure-pipelines.yml
# This is the default single pipeline file

trigger:
  batch: false
  branches:
    include:
      - master

parameters:
- name: buildForAzure
  displayName: 'Build/deploy to Azure cloud?'
  type: boolean
  default: true
- name: deployToCLGuild01
  displayName: 'Deploy to CLGuild01 internal publishing server?'
  type: boolean
  default: true

variables:
  - group: product-readiness.docfx
  - name: gitRepoName
    value: Countries.ja
  - name: gitOwner
    value: pyueh-osi

jobs:
  - template: build-docfx.yml
  - ${{ if eq(parameters.buildForAzure, true) }}:
    - template: pipeline-azure.yml
  - ${{ if eq(parameters.deployToCLGuild01, true) }}:
    - template: pipeline-clguild01.yml
      parameters:
        branchName: $(Build.SourceBranchName)

